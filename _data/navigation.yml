header:
  - titles:
      # @start locale config
      en      : &EN       Home
      # @end locale config
    url: /index.html
    
  - titles:
      # @start locale config
      en      : &EN       Contact
      # @end locale config
    url: /contact.html

